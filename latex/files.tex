\doxysection{File List}
Here is a list of all documented files with brief descriptions\+:\begin{DoxyCompactList}
\item\contentsline{section}{\mbox{\hyperlink{Config_8h}{Config.\+h}} \\*Header file for the \mbox{\hyperlink{classConfig}{Config}} class, responsible for loading and parsing configuration settings from a specified file. The \mbox{\hyperlink{classConfig}{Config}} class provides functionality to load configuration settings from a file, parse key-\/value pairs, and retrieve configuration values as integers or doubles with default values. It serves as a central point for managing configuration settings in the system, allowing other components to access these settings without needing to worry about file parsing or error handling }{\pageref{Config_8h}}{}
\item\contentsline{section}{\mbox{\hyperlink{IPBlocker_8h}{IPBlocker.\+h}} \\*The \mbox{\hyperlink{classIPBlocker}{IPBlocker}} class manages a list of blocked IP address ranges and provides functionality to check if a given IP address is blocked. The class allows adding ranges of IP addresses to the block list and checking if a specific IP address falls within any of the blocked ranges. It uses a vector of pairs to store the blocked IP ranges, where each pair consists of a starting IP address and an ending IP address. The is\+Blocked method iterates through the list of blocked ranges and checks if the given IP address falls within any of the ranges by comparing it to the start and end of each range. Note\+: This implementation assumes that the IP addresses are in a format that allows for simple string comparison (e.\+g., "{} }{\pageref{IPBlocker_8h}}{}
\item\contentsline{section}{\mbox{\hyperlink{LoadBalancer_8h}{Load\+Balancer.\+h}} \\*The \mbox{\hyperlink{classLoadBalancer}{Load\+Balancer}} class manages a pool of servers, a queue of incoming requests, and an IP blocker to handle traffic. It can generate requests, process them through the servers, and dynamically scale the number of servers based on the current load (queue size) }{\pageref{LoadBalancer_8h}}{}
\item\contentsline{section}{\mbox{\hyperlink{Request_8h}{Request.\+h}} \\*The \mbox{\hyperlink{structRequest}{Request}} struct represents an incoming request to the load balancer, containing information about the source and destination IP addresses, the time required to process the request, and the type of job (e.\+g., \textquotesingle{}P\textquotesingle{} for processing or \textquotesingle{}S\textquotesingle{} for storage). The \mbox{\hyperlink{structRequest}{Request}} struct is used to encapsulate the details of a request and is passed through the system as it is processed by the servers in the load balancer. It serves as a fundamental data structure for managing and processing incoming requests in the load balancing system }{\pageref{Request_8h}}{}
\item\contentsline{section}{\mbox{\hyperlink{RequestQueue_8h}{Request\+Queue.\+h}} \\*The \mbox{\hyperlink{classRequestQueue}{Request\+Queue}} class manages a queue of \mbox{\hyperlink{structRequest}{Request}} objects, providing methods to enqueue new requests, dequeue requests for processing, check if the queue is empty, and get the current size of the queue. The \mbox{\hyperlink{classRequestQueue}{Request\+Queue}} class serves as a fundamental component in the load balancing system, allowing for efficient management of incoming requests as they are processed by the servers in the \mbox{\hyperlink{classLoadBalancer}{Load\+Balancer}}. It encapsulates the underlying data structure (a standard queue) and provides a simple interface for interacting with the queue of requests }{\pageref{RequestQueue_8h}}{}
\item\contentsline{section}{\mbox{\hyperlink{Server_8h}{Server.\+h}} \\*The \mbox{\hyperlink{classServer}{Server}} class represents a server in the load balancing system, responsible for processing incoming requests. Each \mbox{\hyperlink{classServer}{Server}} object maintains its own state, including whether it is currently busy, the request it is processing, and the time remaining to complete the request. The \mbox{\hyperlink{classServer}{Server}} class provides methods to assign a new request to the server, process a cycle of work on the current request, check if the server is idle or done with its current request, and clear the completed request from the server\textquotesingle{}s state. This class is used by the \mbox{\hyperlink{classLoadBalancer}{Load\+Balancer}} to manage the processing of requests across multiple servers and to determine when to scale up or down the number of active servers based on the load }{\pageref{Server_8h}}{}
\item\contentsline{section}{\mbox{\hyperlink{Switch_8h}{Switch.\+h}} \\*The \mbox{\hyperlink{classSwitch}{Switch}} class is responsible for routing incoming requests to the appropriate \mbox{\hyperlink{classLoadBalancer}{Load\+Balancer}} based on the type of job specified in the \mbox{\hyperlink{structRequest}{Request}} object. It maintains pointers to both the processing and streaming Load\+Balancers and provides a method to route requests accordingly. The \mbox{\hyperlink{classSwitch}{Switch}} class serves as a central component in the load balancing system, directing traffic to the correct \mbox{\hyperlink{classLoadBalancer}{Load\+Balancer}} based on the job type (e.\+g., \textquotesingle{}P\textquotesingle{} for processing or \textquotesingle{}S\textquotesingle{} for streaming). This allows for efficient handling of different types of requests and helps to optimize the performance of the overall system }{\pageref{Switch_8h}}{}
\end{DoxyCompactList}
